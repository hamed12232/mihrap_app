import 'package:flutter/material.dart';

/// App color constants - Stitch Design Dark Theme
class AppColors {
  AppColors._();

  // Dark Theme - Main Colors
  static const Color background = Color(0xFF050505);
  static const Color surface = Color(0xFF1C1C1E);
  static const Color surfaceLight = Color(0xFF2C2C2E);

  // Card Colors
  static const Color cardWhite = Color(0xFFFFFFFF);
  static const Color cardDark = Color(0xFF1C1C1E);
  static const Color cardGray = Color(0xFF2C2C2E);

  // Text Colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8E8E93);
  static const Color textOnLight = Color(0xFF000000);
  static const Color textOnLightSecondary = Color(0xFF6E6E73);

  // Accent Colors
  static const Color accent = Color(0xFF34C759); // Green accent from design
  static const Color accentBlue = Color(0xFF0A84FF);

  // Border & Divider
  static const Color border = Color(0xFF38383A);

  // Prayer Time Icon Background
  static const Color iconBg = Color(0xFF2C2C2E);

  // Quick Action Button Colors
  static const Color quickActionBg = Color(0xFF2C2C2E);
}
